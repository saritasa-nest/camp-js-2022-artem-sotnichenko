// /** Firebase specific fields. */
// export type FirebaseWrapper<T> = T & {

//   /** Firebase document id. */
//   id: string;
// };

/** Firebase specific fields. */
export interface FirebaseWrapper {

  /** Firebase document id. */
  id: string;
}
