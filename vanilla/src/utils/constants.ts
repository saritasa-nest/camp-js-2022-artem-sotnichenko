export const FORM_ERROR_EMPTY_FIELDS = 'All fields should be filled.';

export const ERROR_FORM_ELEMENTS_ARE_NULL = 'Form elements are not in DOM.';
export const ERROR_PAGINATION_ELEMENTS_ARE_NULL = 'Pagination elements are not in DOM.';
export const ERROR_SORT_ELEMENTS_ARE_NULL = 'Sort elements are not in DOM.';

export const FILMS_COUNT_PER_PAGE = 2;
