import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/** Core module, for domain models and services. */
@NgModule({
  declarations: [],
  imports: [CommonModule],
})
export class CoreModule { }
