// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'artem-sotnichenko-camp',
    appId: '1:403809895371:web:600b2d5fd97f98cc172c4e',
    storageBucket: 'artem-sotnichenko-camp.appspot.com',
    locationId: 'europe-central2',
    apiKey: 'AIzaSyCZD8rCt0GmdzZGCENw8UWRrLFMTMoU9cA',
    authDomain: 'artem-sotnichenko-camp.firebaseapp.com',
    messagingSenderId: '403809895371',
  },
  production: false,
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
